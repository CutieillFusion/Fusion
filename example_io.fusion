# Read a line from the user, print it, and save it to a file.

print("Enter something: ");
let line = read_line();
print(line);

let f = open("out.txt", "w");
write_file(f, line);
close(f);
print("Done.");
