# Native IO: print built-in.
# println(value) — value can be i64, f64, or ptr (string).
# println(value, stream) — optional stream: 0 = stdout, 1 = stderr.

println(42);
println(3.14);
println("hello");

# Optional second argument: stream (1 = stderr).
println("this goes to stderr", 1);
