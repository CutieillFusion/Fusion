# Read a line from the user, print it, and save it to a file.

println("Enter something: ");
let line = read_line();
println(line);

let f = open("out.txt", "w");
write_file(f, line);
close(f);
println("Done.");
