# GET https://example.com and print the status code and response body.

let body = http_request("GET", "https://example.com", "");
let code = http_status();
println("Status:");
println(code);
println("Body:");
println(body);
